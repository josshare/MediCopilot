import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from '@/components/ui/sonner'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'MediCopilot - Medical AI Assistant',
  description: 'AI-powered medical assistant with RAG capabilities for healthcare professionals',
  keywords: ['medical', 'AI', 'assistant', 'healthcare', 'RAG', 'documents'],
  authors: [{ name: 'MediCopilot Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  )
}
